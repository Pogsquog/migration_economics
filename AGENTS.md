# AGENTS.md

Guidance for coding agents working in this repository.

## Repository Context

This repository is trying to re-analyse data from a paper regarding the impact of migration on the economy.

## Commands

No standard build, test, lint, or format commands are defined yet.

If project tooling is added later, update this section with the canonical commands, for example:

- Build:
- Test:
- Lint:
- Format:

## File Handling

- Use `rg` or `rg --files` for discovery when available.
- For large documents, read or process them in chunks.
- Preserve filenames and document metadata unless the task requires changes.

## Change Verification

For documentation-only changes, verify by reviewing the edited file.
For future code changes, run the narrowest relevant checks first, then broader checks when the change affects shared behavior.
